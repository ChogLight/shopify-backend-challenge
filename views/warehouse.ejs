<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css" />
  </head>
  <body>
    <h3>Warehouse list</h3>
    <p class = "text-left">Select the warehouse you want to check
    </p>
    <table class = "table">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">WarehouseName</th>
        </tr>
      </thead>
      <tbody>
        <% for(let count=0; count < warehouses.length; count++) { %>
            <tr>
              <th scope="row">
                <%=count + 1%> 
              </th>
              <td>
                <%=warehouses[count].Name%>
              </td>
              <td>
                <a href ="/check/<%=warehouses[count]._id %>" class="btn btn-success btn-sm>">
                  &nbsp;See Warehouse
              </a>
              </td>
            </tr>
            <% }%>
      </tbody>
    </table>
  </body>
</html>
